/**
 * An object containing the keys used as search parameters
 * To prevent typo when reuse it across components.
 *
 * @property DOCUMENT_TYPE - The key for specifying the document type in search parameters.
 * @property DOCUMENT_NAME - The key for specifying the document name in search parameters.
 */
export const SEARCH_PARAM_KEY = {
  DOCUMENT_TYPE: "documentType",
  DOCUMENT_NAME: "documentName"
}
